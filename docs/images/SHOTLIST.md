# Image shot list

Drop screenshots here (PNG, ~1200px wide). The README references:

- `posts.png` - a grid or single strong example of real published posts from
                @mandemfchq (match graphics + captions). The hero shot.
- `approval.png` - (optional) a Telegram approval prompt for a football draft.
